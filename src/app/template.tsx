import { RouteEntrance } from "~/app/_components/motion-system";

export default function Template({ children }: { children: React.ReactNode }) {
  return <RouteEntrance>{children}</RouteEntrance>;
}
